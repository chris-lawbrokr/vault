
mobile side banner buttons
usage tab 


---
currently I have a bug. If the unread count is a high number or more than 0, it will show the unread count then set it back to zero when the extension leads page is open whoich shouldk trigger setting it    
  back to zero. The poroblem is this resets every 1 min when the user checks for nerw leads. how can I prevent this so that it never resets.    